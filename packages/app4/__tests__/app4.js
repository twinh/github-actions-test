const math = require('@github-test/base');

describe('math', () => {
    test('add', function () {
        expect(math.add(2, 3)).toBe(5);
    });

    test('sub', function () {
        expect(math.add(2, 3)).toBe(5);
    });
});
